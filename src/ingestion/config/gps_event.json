{
    "datasource": "gps_events",
    "dataset": "gps_events",
    "source": {
        "format": "json",
        "options": {
            "multiLine": "true"
        },
        "path": "${LANDING_PATH}/gps_events",
        "schema_location": "${SCHEMA_PATH}/gps_events"
    },
    "sink": {
        "layer": "bronze",
        "format": "parquet",
        "path": "${BRONZE_PATH}/gps_events"
    }
}