{
    "datasource": "historical_incidents",
    "dataset": "historical_incidents",
    "source": {
        "format": "csv",
        "options": {
            "header": "true",
            "inferSchema": "false"
        },
        "path": "${LANDING_PATH}/historical_incidents/historical_incidents.csv",
        "schema_location": "${SCHEMA_PATH}/historical_incidents"
    },
    "sink": {
        "layer": "bronze",
        "format": "parquet",
        "path": "${BRONZE_PATH}/historical_incidents"
    }
}