{
    "datasource": "historical_orders",
    "dataset": "historical_orders",
    "source": {
        "format": "csv",
        "options": {
            "header": "true",
            "inferSchema": "false"
        },
        "path": "${LANDING_PATH}/historical_orders/historical_orders.csv",
        "schema_location": "${SCHEMA_PATH}/historical_orders"
    },
    "sink": {
        "layer": "bronze",
        "format": "parquet",
        "path": "${BRONZE_PATH}/historical_orders"
    }
}