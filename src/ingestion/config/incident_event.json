{
    "datasource": "incident_events",
    "dataset": "incident_events",
    "source": {
        "format": "json",
        "options": {
            "multiLine": "true"
        },
        "path": "${LANDING_PATH}/incident_events"
    },
    "sink": {
        "layer": "bronze",
        "format": "parquet",
        "path": "${BRONZE_PATH}/incident_events"
    }
}