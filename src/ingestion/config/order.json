{
    "datasource": "orders",
    "dataset": "orders",
    "source": {
        "format": "csv",
        "options": {
            "header": "true",
            "inferSchema": "false"
        },
        "path": "${LANDING_PATH}/orders/orders.csv"
    },
    "sink": {
        "layer": "bronze",
        "format": "parquet",
        "path": "${BRONZE_PATH}/orders"
    }
}