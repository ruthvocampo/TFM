{
    "datasource": "routes",
    "dataset": "routes",
    "source": {
        "format": "json",
        "options": {
            "multiLine": "false"
        },
        "path": "${LANDING_PATH}/routes/routes.json"
    },
    "sink": {
        "layer": "bronze",
        "format": "parquet",
        "path": "${BRONZE_PATH}/routes"
    }
}