{
    "datasource": "weather_events",
    "dataset": "weather_events",
    "source": {
        "format": "json",
        "options": {
            "multiLine": "true"
        },
        "path": "${LANDING_PATH}/weather_events"
    },
    "sink": {
        "layer": "bronze",
        "format": "parquet",
        "path": "${BRONZE_PATH}/weather_events"
    }
}